__pycache__/
*.pyc
venv/
.venv/
.idea/
.vscode/
build/
*.spec.bak

# Recurso de versión generado por el CI a partir del tag.
packaging/version_info.txt

# Salida de PyInstaller (la usa el CI como carpeta de trabajo; nunca se versiona).
dist/

# Datos locales (modo portable / caché / registros)
cache/
settings.json
portable
portable.txt
.portable
blendermanager.log
