# Dependencias en tiempo de ejecución.
# La aplicación usa PySide6 (Qt Widgets). Frente a Kivy, Qt renderiza con el
# motor *raster* (CPU) en vez de exigir OpenGL, así que un mismo AppImage
# funciona en todas las distros (no depende del Mesa del sistema).
PySide6-Essentials>=6.7
